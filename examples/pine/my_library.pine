library("my library")

export add(float x, float y) =>
    x + y

export ma(series<float> src, int length = 30) =>
    ta.vwma(src, length)

export custom_rsi(series<float> src, int slow_length = 30, int fast_length = 14) =>
    ta.rsi(src, slow_length) - ta.rsi(src, fast_length)
