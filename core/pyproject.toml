[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "qpace_core"
version = "1.0.0"
classifiers = []

[project.optional-dependencies]

[tool.maturin]
module-name = "qpace_core"
